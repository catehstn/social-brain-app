# Xcode
*.xcodeproj/xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/
# The README and CI both build the MCP tool with -derivedDataPath build
/build/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Test result bundles (CI and the README command write these into the checkout)
*.xcresult

# Scratch space
.claude/scratch/

# Agent worktrees (git repos in their own right — never commit these)
.claude/worktrees/

# OS
.DS_Store
.worktrees
