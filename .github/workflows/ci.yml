name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # So Release Build can be run against a branch on demand — it no longer runs
  # on pull requests. See that job for why.
  workflow_dispatch:

# Cancel superseded runs on the same branch. macOS runners bill at a 10x
# minute multiplier, so redundant runs are the expensive kind.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Show toolchain
        run: xcodebuild -version

      - name: Install xcbeautify
        run: brew list xcbeautify >/dev/null 2>&1 || brew install xcbeautify

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Resolve packages
        run: xcodebuild -resolvePackageDependencies -scheme SocialBrain

      # `shell: bash -eo pipefail` is load-bearing. Without pipefail, piping
      # xcodebuild into a formatter reports the FORMATTER's exit code, so a
      # failed build shows up as a green run — which is exactly how a broken
      # build sat unnoticed on main. Do not remove it.
      - name: Run unit tests
        shell: bash -eo pipefail {0}
        run: |
          xcodebuild test \
            -scheme SocialBrain \
            -destination 'platform=macOS' \
            -only-testing:SocialBrainTests \
            -resultBundlePath TestResults-unit.xcresult \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            | xcbeautify --renderer github-actions

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}-xcresult
          path: '*.xcresult'
          retention-days: 7

  ui-tests:
    name: UI Tests
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Show toolchain
        run: xcodebuild -version

      - name: Install xcbeautify
        run: brew list xcbeautify >/dev/null 2>&1 || brew install xcbeautify

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Resolve packages
        run: xcodebuild -resolvePackageDependencies -scheme SocialBrain-UITests

      - name: Run UI tests
        shell: bash -eo pipefail {0}
        run: |
          xcodebuild test \
            -scheme SocialBrain-UITests \
            -destination 'platform=macOS' \
            -resultBundlePath TestResults-ui.xcresult \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            | xcbeautify --renderer github-actions

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}-xcresult
          path: '*.xcresult'
          retention-days: 7

  # The MCP server is a separate product with its own tests, which had never
  # run before #47 — the target did not exist.
  mcp-server:
    name: MCP Server
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Show toolchain
        run: xcodebuild -version

      - name: Install xcbeautify
        run: brew list xcbeautify >/dev/null 2>&1 || brew install xcbeautify

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          # Two paths: the Release step below uses -derivedDataPath build to
          # run the exact command the README gives people, which puts
          # SourcePackages under build/ rather than the default location.
          #
          # This caches the package *checkout*, not build products — GRDB is
          # still compiled either way. It saves a clone, which is seconds, at
          # the cost of a second archived copy. Kept because the alternative is
          # not running the documented command at all.
          path: |
            ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
            build/SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      # Both actions, because they fail differently: the tests do not exercise
      # DatabaseProxy (they inject a stub store), so only a Release build of the
      # tool proves the binary the README tells people to build still links.
      - name: Run MCP tests
        shell: bash -eo pipefail {0}
        run: |
          xcodebuild test \
            -scheme SocialBrainMCP \
            -destination 'platform=macOS' \
            -resultBundlePath TestResults-mcp.xcresult \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            | xcbeautify --renderer github-actions

      - name: Build the documented Release binary
        shell: bash -eo pipefail {0}
        run: |
          xcodebuild build \
            -scheme SocialBrainMCP \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            | xcbeautify --renderer github-actions
          test -x build/Build/Products/Release/SocialBrainMCP

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}-xcresult
          path: '*.xcresult'
          retention-days: 7

  # Not on pull requests — on main, and on demand via workflow_dispatch.
  #
  # Measured: this job is ~40 of the ~130 billed macOS-minutes a full run costs,
  # the single largest share, and macOS runners bill at a 10x multiplier against
  # a ~200-minute monthly allowance. It compiles the same source the other jobs
  # already compile, differing only in configuration, so the marginal thing it
  # catches on a PR is a Release-only build failure — real, but rare, and caught
  # on main within minutes of the merge.
  #
  # The trade is deliberate (#91): a Release-only break reaches main and is
  # fixed forward, rather than every PR push paying to rule it out.
  release-build:
    name: Release Build
    runs-on: macos-15
    timeout-minutes: 30
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v5

      - name: Show toolchain
        run: xcodebuild -version

      - name: Install xcbeautify
        run: brew list xcbeautify >/dev/null 2>&1 || brew install xcbeautify

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Build Release configuration
        shell: bash -eo pipefail {0}
        run: |
          xcodebuild build \
            -scheme SocialBrain \
            -destination 'platform=macOS' \
            -configuration Release \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            | xcbeautify --renderer github-actions
