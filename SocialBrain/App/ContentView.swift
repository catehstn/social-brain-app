import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Social Brain")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
